'use strict';

describe('coderado.profile module', function() {

  beforeEach(module('coderado.views.profile'));

  describe('profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ViewProfileCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});