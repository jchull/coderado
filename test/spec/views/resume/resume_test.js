'use strict';

describe('coderado.resume module', function() {

  beforeEach(module('coderado.views.resume'));

  describe('profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('ViewResumeCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});