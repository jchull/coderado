'use strict';

describe('coderado.contact module', function() {

  beforeEach(module('coderado.contact.contact'));

  describe('profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('ViewContactCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});