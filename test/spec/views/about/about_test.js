'use strict';

describe('coderado.about module', function() {

  beforeEach(module('coderado.views.about'));

  describe('profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('ViewAboutCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});